deploy:
  environment: production
  stage: deploy
  tags:
    - windows
    - cmd
  only:
    - master
  script:
    - gem install dpl
    - dpl --provider=heroku --app=%HEROKU_APP_NAME% --api-key=%HEROKU_API_KEY%
    - echo Deploy %HEROKU_APP_NAME%